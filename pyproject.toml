[project]
name = "mar-15"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "jupyter>=1.1.1",
    "training-hub[lora]>=0.6.0",
    "wandb>=0.25.1",
]
